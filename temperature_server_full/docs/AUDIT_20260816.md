# コード監査 2026-08-16 (Phase 2)

前回監査 (`AUDIT_20260717.md`) から 1 か月、機能追加 (nickname / masters / camera 復活 / パフォーマンスタブ) と再イテレーションで入り込んだ死コードを整理。

## 削除したファイル (17 個)

### Python モジュール (完全デッド、外部参照ゼロ)

| ファイル | 削除理由 |
|---|---|
| `app/routes/api_improved.py` | 中身 DEPRECATED コメントのみのスタブ |
| `app/routes/wifi.py` | wifi_manager 依存、ENABLE_WIFI_API フラグ廃止で不要 |
| `safe_update_raspberry.py` | 廃止済スタブ |
| `services/background_tasks.py` | `BackgroundTaskManager.start()` を呼ぶ箇所ゼロ、モジュール丸ごとデッド |
| `services/wifi_manager.py` | hostapd/dhcpcd 直操作は Trixie/NetworkManager 実運用と食い違うため撤廃 |
| `utils/health_check.py` | `HealthChecker` 呼出ゼロ |
| `utils/validators.py` | `validate_*` 呼出ゼロ (api.py はインライン検証) |
| `utils/error_handler.py` | `handle_errors` デコレータ適用ゼロ |
| `utils/exceptions.py` | 上記 2 ファイルからしか import されず実質デッド |
| `cli/management_cli.py` | wifi_manager 依存 + init-db 分岐バグ、bash 直叩き想定でどこからも wire されず |
| `systemd/install_service.sh` | `temp-manage` シンボリックリンク作成用、CLI 削除に伴い不要 (実際のインストーラは `install.sh`) |
| `tests/test_api_endpoints.py` | パスバグ + 全ケース失敗、フィールド名不一致 |

### テンプレート

| ファイル | 削除理由 |
|---|---|
| `templates/video_feed_frame.html` | 参照ゼロスタブ、前回監査で削除決定済だったが放置 |
| `templates/test_api.html` | `/test` ルート削除に伴い不要 |

### ドキュメント (wifi 系)

- `docs/DIAGNOSE_WIFI_ISSUE.md`
- `docs/TROUBLESHOOTING_WIFI_AP_SETUP.md`
- `docs/WIFI_SETUP.md`

いずれも `wifi_manager` の使い方や関連トラブル対処を書いていたが、実運用は
Pi 側の netplan/NetworkManager (LAN) + hostapd/dnsmasq (AP) の永続化設定に
移行しており、Flask 内 WiFi 操作は使わない。関連ドキュメントは
`outputs/RaspberryPi_AP_Setup/` と `outputs/docs/RaspberryPi_セットアップガイド.md` に統合。

## コード修正 (削除以外)

### 未使用 import 除去 (10 件)

| ファイル | 除去した import |
|---|---|
| `services/serial_reader.py` | `from datetime import datetime`、`from pathlib import Path` |
| `logger.py` | `from pathlib import Path` |
| `cli/serial_test.py` | `SerialReader` (`create_serial_reader` のみ利用) |

### 参照ゼロ関数削除

| ファイル | 関数 |
|---|---|
| `database/queries.py` | `TemperatureQueries.get_latest_reading()` (get_all_latest が代替) |
| `database/queries.py` | `NicknameQueries.get()` |
| `database/queries.py` | `TemperatureQueries.delete_test_sensors()` (テストタブ削除で不要) |

### 参照ゼロ Flask ルート削除

| ルート | 経緯 |
|---|---|
| `POST /api/delete-test-sensors` | 2026-08-16 テストセンサータブ削除で参照ゼロ |
| `POST /api/dashboard/combined` | 未使用、`/api/temperature/batch` が実装済 |
| `GET /test` | デバッグ用、公開版では不要 |

### DB スキーマ (`database/models.py`)

未使用 CREATE 文を撤廃 (**既存 DB の該当テーブルは無害なため DROP しない、新規 DB のみ影響**):

- `wifi_connections` — 書込みコードなし
- `system_logs` — SystemLogQueries 廃止済 (常時空)
- `temperature_alerts` — アラート機構未実装
- `settings` — Python 側から参照ゼロ (定数化されている)

### バグ修正

- `cli/management_cli.py:280-282` — init-db 分岐が誤って reboot を呼ぶバグ + 到達不可能な重複 elif → ファイル自体を削除したので副次的解消

### コメントブロック除去

- `database/queries.py` の SystemLogQueries 廃止説明ブロック (旧監査記録) を整理、`MasterQueries` の位置に説明ヘッダを移動

## 結果

- **Python ファイル数**: 削減 (詳細は git log 参照)
- **`utils/` の稼働モジュール**: `request_tracing.py` のみ (それ以外は全撤廃)
- **`services/` の稼働モジュール**: `serial_reader.py` のみ (background_tasks / wifi_manager 撤廃)
- **`app/routes/`**: `api.py` + `dashboard.py` の 2 個 (wifi.py / api_improved.py 撤廃)
- **`cli/`**: `serial_test.py` のみ (management_cli.py 撤廃)
- **`tests/`**: 空 (壊れたテストのみだったので撤廃)
- **`docs/`**: wifi 系 3 ファイル削除

すべての Python ファイルが構文チェックをパスすることを確認済。
