# コード監査レポート 2026-07-17

## 監査対象

- `temperature_server/` 一式 (Z:\afterNK\ESPSARVER\outputs\temperature_server_full)
- 監査者: Claude (AI アシスタント)
- 監査時点の総行数: 13,682 行 (Python + HTML)

## 監査背景

先の CODE_AUDIT_REPORT.md (XSS/SQL/リソース管理等の指摘) は既に修正済み。
今回は「冗長・重複・デッドコード」観点で見直し。

## 項目一覧と実施結果

| # | 項目 | 実施結果 | 削減行数 |
|---|---|---|---|
| 1 | `app/routes/api_improved.py` (345 行、未登録デッドコード) | stub 化 (削除は Windows/Git 側で) | -336 |
| 2 | `run.py` の main() 重複定義 (line 32 のダミー) | 削除・整理 | -60 |
| 3 | `dashboard.py /test` の inline HTML と `test_api.html` の重複 | render_template 化 | -30 |
| 4 | CORS の三重定義 (r"/*" が上書きしていた) | r"/*" 1 本に集約 | -2 |
| 5 | before_request の Header 全ダンプ、DELETE 詳細ログ (冗長) | 1 行フォーマットに軽量化 | -15 |
| 6 | `dashboard.py` のカメラ MJPEG 系 (opencv 依存、未使用) | 削除 (33 行に圧縮) | -159 |
| 7 | `/wifi/*` blueprint (WiFiManager 依存、本用途不要) | ENABLE_WIFI_API 環境変数で制御、デフォルト無効 | 0 (フラグ化) |
| 8 | `services/serial_reader` の無条件 import | SERIAL_ENABLED=True の時のみ遅延 import | 0 (安全性向上) |
| 9 | `/api/test-delete` (デバッグ用) | 削除、management.html のプリチェックも削除 | -30 |
| 10 | 各エンドポイントの手作り `uuid.uuid4()[:8]` | `@trace_request` デコレータ + `get_request_id()` に統一 | -88 (api.py) |
| 11 | `SystemLogQueries` (常時空、insert_log 未呼び出し) | クラス削除、`/api/logs` `/api/export/logs` はログファイル直読に変更 | -40 (queries.py) |
| 12 | `templates/video_feed_frame.html` (参照ゼロ) | stub 化 | -20 |

**合計削減: 約 780 行 (13,682 行の 5.7%)**

## 変更ファイル一覧

- `app/__init__.py`: 157 → 126 行
- `app/routes/api.py`: 679 → 591 行
- `app/routes/api_improved.py`: 345 → 9 行 (stub)
- `app/routes/dashboard.py`: 192 → 33 行
- `database/queries.py`: 389 → 349 行
- `run.py`: 192 → 132 行
- `services/background_tasks.py`: SystemLogQueries 依存を stub 化
- `cli/management_cli.py`: SystemLogQueries import 削除
- `templates/management.html`: /api/test-delete プリチェック削除
- `templates/test_api.html`: 元のインラインを template 化
- `templates/stream.html`: stub 化 (/stream ルート削除に伴い)
- `templates/video_feed_frame.html`: stub 化 (参照ゼロ)

## 残置決定 (削除しなかったもの)

- `database/models.py` の system_logs テーブル DDL: 既存 DB マイグレーション互換のため残置。DDL のみで実運用データはゼロ
- `services/wifi_manager.py`, `app/routes/wifi.py`: ENABLE_WIFI_API=True で復活可能なフラグ制御のみ (削除ではない)
- `templates/dashboard.html` (74KB): ダッシュボード本体、機能監査は別途

## 後続タスク

- Item 10 の `utils/validators.py` `utils/exceptions.py` `utils/error_handler.py` 活用は本監査では未実施 (別途 refactor 予定)
- stub 化した 3 ファイル (api_improved.py, stream.html, video_feed_frame.html) は Windows Explorer での完全削除を推奨
- 新機能: デバイス nickname 管理 (device_nicknames テーブル + 管理 UI)

## 動作影響

- `/api/temperature` (ESP POST 経路): 変更なし。ESP スケッチ改修不要
- `/api/sensors` (ダッシュボード用): 変更なし
- `/api/logs`: DB 空だったのがログファイル tail に変更 → 実データが返るようになる (改善)
- `/api/export/logs`: 同上
- `/api/test-delete`: 削除。management.html のプリチェックも削除済 (DELETE 動作に影響なし)
- `/stream` (カメラ): 削除
- `/wifi/*`: ENABLE_WIFI_API=True にしない限り 404
